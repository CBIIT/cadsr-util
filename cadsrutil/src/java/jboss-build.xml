<!DOCTYPE project>
<!--
  Contexttree Ant build 
  =============================================

  Author: Sumana Hegde
  

  This build script was created using Ant version 1.6 and Java 1.4.2_06
-->

<project name="contexttree" default="init" basedir=".">
  
  <property name="contexttree.version" value="3.2.0.3" />
  <property name="contexttree.build" value="1" />
  <property name="project.home" value="${basedir}/../.."/>
  <property name="app.name" value="cdebrowser" />
    
  <!-- Java Source Directories -->
  <property name="java.src.dir" value="${project.home}/src/java"/>
    
  <!-- ui Directories -->
  <property name="build.dir" value="${project.home}/build"/>
  <property name="dist.dir" value="${project.home}/dist"/>
  <property name="classes.build.dir" value="${build.dir}/classes"/>
  <property name="java.lib.dir" value="${project.home}/lib/java"/>

  <!-- =========== CLEAN BUILD AREA ======================= -->
    <target name="clean">
          <delete dir="${classes.build.dir}" quiet="false"/>
          <delete dir="${build.dir}" quiet="false"/>   
          <delete dir="${dist.dir}" quiet="false"/>          
    </target>
    
    <target name="clean_build_dir">
          <delete dir="${classes.build.dir}" quiet="false"/>
          <delete dir="${build.dir}" quiet="false"/>           
    </target>
 <!-- =========== END CLEAN BUILD AREA ======================= -->
 
 
 <!-- =========== CONFIGURATION ======================= -->
   <!-- Initialize the system -->
   <target name="init">
     <!-- Create build dir -->
     <!-- Create time stamp -->
     <tstamp/>
     <mkdir dir="${build.dir}"/>
     <mkdir dir="${classes.build.dir}"/>
     <mkdir dir="${dist.dir}"/>
   </target>
   <!-- end init -->
 <!-- =========== END CONFIGURATION ======================= -->


   <!--Set the lib classpath-->
   <path id="classpath">
    <fileset dir="${java.lib.dir}">
      <include name="*.jar"/>
      <include name="*.zip"/>
    </fileset>
   </path>
   
   <path id="srcpath">
         <pathelement location="${java.src.dir}"/>
   </path>
   
   <target name="compile" depends="init">
      <javac destdir="${classes.build.dir}" debug="on" >
         <classpath refid="classpath"/>
         <src refid="srcpath"/>
      </javac>  
   </target>
   
	<!-- ============ CREATE ctontexttree JAR FILE ================== -->
	<target name="jar_contexttree" depends="clean,compile">
	    <jar jarfile="${dist.dir}/contexttree.jar">
	      <fileset dir="${classes.build.dir}">
	        <patternset>
	        	<include name="gov/nih/nci/ncicb/**/*.*"/>	        
	        </patternset>
	      </fileset>           
           <fileset dir="${java.src.dir}">
             <patternset>
               <include name="gov/nih/nci/ncicb/cadsr/common/persistence/**/*.xml"/>
             </patternset>
           </fileset>      
	    </jar>
	  </target>
	  <!-- =========== END ctontexttree JAR PROCESS ======================= -->

</project>